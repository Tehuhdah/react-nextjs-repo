// Section 1, vid 6: Editting The First App

// This process basically sets up a new page in Next.JS
// Import the React library (implicitly done in Next.js)

// Define a new functional component called AwesomePage
export default function AwesomePage() {
  // The component returns some JSX to render
  return (
    // The main HTML element is used to wrap the main content of the document
    <main>
      <h1>NextJS Is an Awesome Page!</h1>
    </main>
  );
}
